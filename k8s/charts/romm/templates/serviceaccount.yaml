{{- if .Values.romm.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "romm.serviceAccountName" . }}
  labels:
    {{- include "romm.labels" . | nindent 4 }}
  {{- with .Values.romm.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
