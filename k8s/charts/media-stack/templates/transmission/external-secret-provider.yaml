{{- if .Values.transmission.provider.user.externalSecret }}
{{- with .Values.transmission.provider.user.externalSecret }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "transmission.fullname" $ | printf "vpn-credentials-%s" }}
spec:
  secretStoreRef: {{- .secretStoreRef | toYaml | nindent 4 }}
  target:
    name: {{ include "transmission.fullname" $ | printf "vpn-credentials-%s" }}
  data:
    - secretKey: user
      remoteRef:
        key: {{ .secret.userKey }}
    - secretKey: password
      remoteRef:
        key: {{ .secret.passKey }}
{{- end }}
{{- end }}
