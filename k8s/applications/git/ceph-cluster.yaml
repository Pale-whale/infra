app:
  name: ceph-cluster
  project: infra
  source:
    repoURL: git@github.com:rook/rook.git
    targetRevision: v1.19.6
    path: deploy/charts/rook-ceph-cluster
  destination:
    server: https://kubernetes.default.svc
    namespace: rook-ceph
  helm:
    values: |
      operatorNamespace: rook-ceph
      clusterName: homelab
      cephImage:
        tag: v19.2.3
        allowUnsupported: false
      toolbox:
        enabled:
          true
      monitoring:
        enabled: true
      cephClusterSpec:
        # NEVER change this value
        dataDirHostPath: /var/lib/rook
        mon:
          count: 3
          allowMultiplePerNode: false
          volumeClaimTemplate:
            spec:
              storageClassName: proxmox-nvme-xfs
              resources:
                requests:
                  storage: 10Gi
        mgr:
          count: 2
          allowMultiplePerNode: false
          modules:
            - name: rook
              enabled: true
        dashboard:
          enabled: true
          ssl: false
        network:
          connections:
            encryption:
              enabled: false
            compression:
              enabled: true
            requireMsgr2: true
        logCollector:
          enabled: true
          periodicity: daily
          maxLogSize: 500M
        skipUpgradeChecks: false
        continueUpgradeAfterChecksEvenIfNotHealthy: false
        # OSDs are consumer WD SA500 SATA SSDs (no power-loss protection) passed
        # via virtio, all on a single Proxmox node. Their O_DSYNC latency is fine
        # at low queue depth but collapses (aio_wait seen up to ~6s) under
        # concurrent background IO, freezing client-facing workloads. mclock's
        # auto-benchmark also wildly overestimates their capacity (~56-77k IOPS),
        # so the default 'balanced' profile let too much scrub/recovery IO through.
        # Prioritise client IO and cap concurrent scrubs. Applied cluster-wide via
        # `ceph config set osd ...`.
        cephConfig:
          # Point the Ceph dashboard at the in-cluster kube-prometheus-stack so its
          # monitoring/performance panels render (otherwise the dashboard shows
          # "Please consult the documentation on how to configure and enable the
          # monitoring functionality."). These are the exact options that
          # `ceph dashboard set-prometheus-api-host` writes (mgr/dashboard/*),
          # applied here via Rook's cephConfig so the setting is GitOps-managed.
          # SSL_VERIFY is false because the target is plain HTTP in-cluster.
          mgr:
            "mgr/dashboard/PROMETHEUS_API_HOST": "http://kube-prometheus-stack-prometheus.kube-prometheus-stack.svc:9090"
            "mgr/dashboard/PROMETHEUS_API_SSL_VERIFY": "false"
          osd:
            osd_mclock_profile: "high_client_ops"
            osd_max_scrubs: "1"
        placement:
          mon:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: Exists
            tolerations:
              - key: "node-role.kubernetes.io/control-plane"
                operator: "Exists"
                effect: "NoSchedule"
          mgr:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: Exists
            tolerations:
              - key: "node-role.kubernetes.io/control-plane"
                operator: "Exists"
                effect: "NoSchedule"
        resources:
          mon:
            limits:
              memory: 2Gi
            requests:
              cpu: 500m
              memory: 1Gi
          mgr:
            limits:
              memory: 2Gi
            requests:
              cpu: 300m
              memory: 512Mi
          osd:
            limits:
              memory: "6Gi"
            requests:
              cpu: "1"
              memory: "6Gi"
        disruptionManagement:
          managePodBudgets: true
          osdMaintenanceTimeout: 30
        storage:
          useAllNodes: false
          useAllDevices: false
          allowDeviceClassUpdate: true
          nodes:
            - name: "agent-milo"
              devices:
                - deviceClass: ssd
                  name: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi2"
                - deviceClass: ssd
                  name: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi3"
            - name: "agent-dewey"
              devices:
                - deviceClass: ssd
                  name: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi2"
                - deviceClass: ssd
                  name: "/dev/disk/by-id/scsi-0QEMU_QEMU_HARDDISK_drive-scsi3"
            - name: "agent-rupert"
              devices:
                - deviceClass: nvme
                  name: "/dev/disk/by-id/nvme-Samsung_SSD_9100_PRO_8TB_S7YHNJ0Y903438J"
      ingress:
        dashboard:
          annotations:
            traefik.ingress.kubernetes.io/router.middlewares: traefik-traefik-forward-auth@kubernetescrd
          host:
            name: ceph.palewhale.fr
            path: "/"
            pathType: Prefix
      cephBlockPools:
        - name: ceph-blockpool
          spec:
            failureDomain: host
            replicated:
              size: 2
          storageClass:
            enabled: true
            name: ceph-block
            isDefault: true
            reclaimPolicy: Delete
            allowVolumeExpansion: true
            volumeBindingMode: "Immediate"
            parameters:
              imageFormat: "2"
              imageFeatures: layering,fast-diff,object-map,deep-flatten,exclusive-lock
              csi.storage.k8s.io/provisioner-secret-name: rook-csi-rbd-provisioner
              csi.storage.k8s.io/provisioner-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/controller-expand-secret-name: rook-csi-rbd-provisioner
              csi.storage.k8s.io/controller-expand-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/node-stage-secret-name: rook-csi-rbd-node
              csi.storage.k8s.io/node-stage-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/fstype: ext4
      cephFileSystems:
        - name: ceph-filesystem
          spec:
            metadataPool:
              replicated:
                size: 3
            dataPools:
              - failureDomain: host
                replicated:
                  size: 2
                name: data0
            metadataServer:
              activeCount: 1
              activeStandby: true
              resources:
                limits:
                  memory: "4Gi"
                requests:
                  cpu: "1"
                  memory: "4Gi"
              priorityClassName: system-cluster-critical
          storageClass:
            enabled: true
            isDefault: false
            name: ceph-filesystem
            pool: data0
            reclaimPolicy: Delete
            allowVolumeExpansion: true
            volumeBindingMode: "Immediate"
            mountOptions:
              - ms_mode=secure
            parameters:
              csi.storage.k8s.io/provisioner-secret-name: rook-csi-cephfs-provisioner
              csi.storage.k8s.io/provisioner-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/controller-expand-secret-name: rook-csi-cephfs-provisioner
              csi.storage.k8s.io/controller-expand-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/controller-publish-secret-name: rook-csi-cephfs-provisioner
              csi.storage.k8s.io/controller-publish-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/node-stage-secret-name: rook-csi-cephfs-node
              csi.storage.k8s.io/node-stage-secret-namespace: "{{ .Release.Namespace }}"
              csi.storage.k8s.io/fstype: ext4
      cephObjectStores: {}
